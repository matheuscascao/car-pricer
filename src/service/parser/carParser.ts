// checks for the integrity of data before upserting it into the database

import { CarType } from "../../entities/carEntity";

function parseCar(carData: CarType): boolean {
    //check for data integrity ?

    return true
}