// function normalizeStringToFloat(value: string): number {
//     const numericString = value.replace(/[^\d.-]/g, '');    
//     const floatValue = parseFloat(numericString);
    
//     return floatValue;
// };

// export default normalizeStringToFloat;